create user if not exists 'ki-vbrcw-x'@'localhost' identified by '~ki-vbrcw-x-password~';
grant  select
 , insert
 , update
 , delete
 , create
 , create routine
 , alter routine
  on vbrescuecouncilwebsitedb_x.*
  to 'ki-vbrcw-x'@'localhost'
;
--
-- Must grant PROCESS priv to primary user to include SHOW ENGINE INNODB
-- STATUS output in MySQL-related exception reports.
--
;
grant process
  on *.*
  to 'ki-vbrcw-x'@'localhost'
